<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LaundryDay" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/switchActivate</item>
        <item name="colorPrimaryVariant">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/switchActivate</item>
        <item name="colorSecondaryVariant">@color/switchBackground</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="actionBarTheme">@style/ActionBarStyle</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@android:color/black</item>
        <item name="android:buttonStyle">@style/CustomButtonStyle</item>
        <item name="ratingBarStyle">@style/RatingBarStyle</item>
    </style>

    <style name="ActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="actionMenuTextColor">@color/switchActivate</item>
    </style>

    <style name="CustomButtonStyle" parent="android:Widget.Material.Light.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="backgroundTint">@color/switchActivate</item>
    </style>

    <style name="BottomButtonStyle" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">17sp</item>
        <item name="android:height">70dp</item>
    </style>

    <style name="Theme.LaundryDay.ActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <!--<item name="android:titleTextStyle">@style/Theme.LaundryDay.ActionBarStyle.TitleTextStyle</item>-->
    </style>

    <style name="Theme.LaundryDay.ActionBarStyle.TitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/bottomButtonColor</item>
    </style>

    <style name="LoginButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/login_button</item>
        <item name="android:layout_marginVertical">3dp</item>
        <item name="android:textAlignment">textStart</item>
        <item name="android:paddingStart">68dp</item>
    </style>

    <style name="RatingBarStyle" parent="@android:style/Widget.Material.Light.RatingBar">
        <item name="android:progressTint">@color/ratingBarColor</item>
        <item name="android:secondaryProgressTint">@color/ratingBarColor</item>
    </style>
</resources>